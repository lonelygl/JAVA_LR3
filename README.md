# Система управления лифтами

## Реализованные требования 

**Архитектура системы**

| Компонент | Реализация |
|-----------|------------|
| Лифты (M потоков) | Класс `Elevator`, реализующий `Runnable` | 
| Диспетчер | Класс `Dispatcher`, отдельный поток | 
| Генератор запросов | Класс `PassengerGenerator` | 
| GUI интерфейс | Класс `ElevatorFrame` на Swing |

 **Функциональность лифтов**

| Требование | Реализация | 
|------------|------------|
| Текущий этаж | `currentFloor` в классе `Elevator` | 
| Направление движения | `Direction` enum (UP, DOWN, IDLE) |
| Статус | `ElevatorStatus` enum (MOVING, STOPPED, DOORS_OPEN) | 
| Очередь целей | `internalTargets`, `externalTargets` | 
| Остановка на этажах | Метод `handleStop()` | 
| Открытие/закрытие дверей | Методы `openDoors()`, `closeDoors()` | 
| Выполнение запросов | Полный цикл: вызов → посадка → доставка | 

 **Дополнительный функционал**
В рамках лабораторной работы был реализован дополнительный функционал:
- Поддержка как ручного ввода пользователем с обработкой ошибок, так и автогенерации вызовов лифтов
- Графический интерфейс GUI
- Оптимизация выбора лифта (система взвешенной оценки лифтов с учётом текущего направления движения, количества целей, расстояния до пассажира, необходимости разворота (вверх/вниз))
- добавление временных меток, расстояния до пассожира, приблизительного времени ожидания лифта в логи
- статистика для диспетчера и каждого лифта в отдельности










**Структура проекта**
```
elevator-system/
├── src/
│   ├── control/           # диспетчер и генератор
│   ├── model/            # логика работы лифта, направления, статус
│   ├── util/             # логирование
│   └── gui/              # интерфейс
└── README.md             
```





